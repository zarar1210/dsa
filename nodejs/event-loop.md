## **Event Loop**

The event loop in Node.js is a mechanism that allows asynchronous tasks to be handled efficiently without blocking other operations.

- Executes JavaScript synchronously first and then processes asynchronous operations.
